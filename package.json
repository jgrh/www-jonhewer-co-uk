{
  "version": "1.6.0",
  "private": true,
  "scripts": {
    "build": "eslint scripts/index.js plugins templates/helpers.js && lerret generate && compass compile . --sass-dir=css --css-dir=target/css && mkdir -p ./target/scripts && browserify -g uglifyify ./scripts/index.js -o ./target/scripts/bundle.js && cp -r ./fonts ./target",
    "clean": "rm -rf ./target && mkdir ./target",
    "clean-build": "npm run clean && npm run build"
  },
  "dependencies": {
    "domready": "^1.0.8",
    "element-resize-event": "^2.0.9",
    "imagesloaded": "^4.1.0"
  },
  "devDependencies": {
    "bluebird": "^3.5.0",
    "browserify": "^14.4.0",
    "eslint": "^4.0.0",
    "json": "^9.0.6",
    "lerret": "^0.4.8",
    "lodash": "^4.17.4",
    "uglifyify": "^4.0.0"
  }
}
